import type { Locale } from './type'

const enUS: Locale = {
  think: {
    loading: 'Thinking...',
    done: 'Done',
  },
  sender: {
    placeholder: 'Type a message...',
    send: 'Send',
  },
}

export default enUS
